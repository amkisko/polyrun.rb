{
  "name": "polyrepo_demo",
  "private": true,
  "scripts": {
    "build": "npm run build:admin && npm run build:store && npm run build:portal",
    "build:admin": "vite build --config admin/vite.config.ts",
    "build:store": "vite build --config store/vite.config.ts",
    "build:portal": "vite build --config portal/vite.config.ts"
  },
  "devDependencies": {
    "playwright": "^1.50.0",
    "typescript": "^5.6.0",
    "vite": "^6.4.2"
  }
}
