The **runnable** multi-app demo is **`../polyrepo_demo/`** (one Rails app, three Vite client folders: `admin/`, `store/`, `portal/`).

This `apps/` directory is reserved for documentation-only stubs if you split into **multiple** Rails engines or repos later. For Polyrun **`run-shards`** and **`merge-coverage`**, point **`partition.paths_file`** at **`../polyrepo_demo/spec/paths.txt`** (see **`../polyrun.yml`**).
