{
  "private": true,
  "scripts": {
    "build": "npm run build:admin && npm run build:store",
    "build:admin": "vite build --config admin/vite.config.ts",
    "build:store": "vite build --config store/vite.config.ts"
  },
  "devDependencies": {
    "playwright": "^1.50.0",
    "typescript": "^5.6.0",
    "vite": "^6.4.2"
  }
}
